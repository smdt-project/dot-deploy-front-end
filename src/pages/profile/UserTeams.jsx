import { useSelector } from "react-redux";

const UserTeams = ({ isLoggedInUser }) => {
 // const { userData } = useSelector((state) => state.profile);
	return(
		<div>
			User Teams
		</div>
	)
}
export default UserTeams;
